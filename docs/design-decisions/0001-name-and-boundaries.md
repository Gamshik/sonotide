# ADR 0001: Sonotide Name And Boundaries

Sonotide was chosen as the repo/package name because it is audio-linked, mature-sounding, and usable as a namespace. The library boundary stops at Windows audio infrastructure. It does not own decoding, equalization, or app-specific transport policy.

